# Local dev: leave empty (Vite proxy forwards /api to localhost:3000)
VITE_API_URL=

# Production (Render): set to your deployed backend URL, no trailing slash
# VITE_API_URL=https://your-app.onrender.com
